## KEYS

When using in the jar file, use this:


- server.id -> server name in the responses, NULLABLE
- directory.name -> directory name, == NOT NULLABLE ==
- file.name -> file name, == NOT NULLABLE ==