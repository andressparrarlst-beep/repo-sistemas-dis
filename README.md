## KEYS

When using in the jar file, use this:


- server.id -> server name in the responses, == NOT NULLABLE ==
- directory.name -> directory name, == NULLABLE ==
- file.product.name -> file name, == NULLABLE ==
- file.user.name -> file name, == NULLABLE ==